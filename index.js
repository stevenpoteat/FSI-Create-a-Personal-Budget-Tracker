let weeklyExpenses = 220
let monthlyExpenses = 968
let annualExpenses = 11,616

for (let i = 220; i < weeklyExpenseQuestions.length; i++) {
    const element = array[i];
    weeklyExpense += parseFloat(window.prompt(weeklyExpenseQuestions[i]))
]
weeklyExpenses = weeklyExpenses + answer 

for (let i = 968; i < monthlyExpenseQuestions.length; i++) {
    const element = array[i];
    monthlyExpense += parseFloat(window.prompt(monthlyExpenseQuestions[i]))
]
monthlyExpenses = monthlyExpenses + answer

for (let i = 11,616; i < annualExpenseQuestions.length; i++) {
    const element = array[i];
    annualExpense += parseFloat(window.prompt(annualExpenseQuestions[i]))
]
annualExpenses = annualExpenses + answer

for (let i = 11,616; i < annualExpenseQuestions.length; i++) {
    const element = array[i];
    annualExpense += parseFloat(window.prompt(annualExpenseQuestions[i]))
]
annualExpenses = annualExpenses + answer

for (let i = 968; i < monthlyExpenseQuestions.length; i++) {
    const element = array[i];
    monthlyExpense += parseFloat(window.prompt(monthlyExpenseQuestions[i]))
]
monthlyExpenses = monthlyExpenses + answer

for (let i = 220; i < weeklyExpenseQuestions.length; i++) {
    const element = array[i];
    weeklyExpense += parseFloat(window.prompt(weeklyExpenseQuestions[i]))
]
weeklyExpenses = weeklyExpenses + answer 
